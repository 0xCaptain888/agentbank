import { BigInt, Bytes } from "@graphprotocol/graph-ts";

export function handleValidationRequested(event: any): void {
  // Create Validation entity with Pending status
}

export function handleValidationResponded(event: any): void {
  // Update Validation entity with response status
  // Update agent validation stats
}
